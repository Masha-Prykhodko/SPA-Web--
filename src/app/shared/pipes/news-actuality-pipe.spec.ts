import { NewsActualityPipe } from './news-actuality-pipe';

describe('NewsActualityPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsActualityPipe();
    expect(pipe).toBeTruthy();
  });
});
